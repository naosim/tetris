/**
 * @typedef ForEachBlocksCallback
 * @type {(x:number, y:number, value: number) => void}
 */

/**
 * @typedef BlocksRef
 * @property { (cb: ForEachBlocksCallback) => void } forEachBlocks
 * @interface
 */

/**
 * @typedef Pad
 * @property {boolean} rightKeyIsDown
 * @property {boolean} leftKeyIsDown
 * @property {boolean} downKeyIsDown
 * @property {boolean} rotationButtonIsDown
 */

/**
 * @typedef {{x:number, y:number}} Pos
*/

export const types = {}